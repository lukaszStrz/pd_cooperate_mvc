@model Cooperate_mvc.Models.Group

@{
    ViewBag.Title = "Edycja grupy";
}
<div class="realContent">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)

        <fieldset>
            <legend>Edycja grupy</legend>

            <div class="editor-label">
                @Html.Label("Nazwa")
            </div>
            <div class="editor-field">
                @Html.EditorFor(model => model.Group_name)
                @Html.ValidationMessageFor(model => model.Group_name)
            </div>

            <div class="editor-label">
                @Html.Label("Opis")
            </div>
            <div class="editor-field">
                @Html.EditorFor(model => model.Group_description)
                @Html.ValidationMessageFor(model => model.Group_description)
            </div>

            <p>
                <input type="submit" value="Zapisz" />
            </p>
        </fieldset>
    }

    <div>
        @Html.ActionLink("Wróć do listy", "Index")
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
